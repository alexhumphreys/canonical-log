// Configured entirely by the root build: see the `canonical-log-bom` branch in
// build.gradle.kts, which applies java-platform and derives its constraints from the
// module list in settings.gradle.kts.
